# Binaries
/dbbat
*.priv*
*.lock

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/
node_modules/
bun.lock

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment files
.env
*.key

# Browser network captures. A HAR records whole requests — Authorization
# headers, session cookies, tokens in URLs — so it is a credential file even
# when it looks like a debugging artifact. Never commit one.
*.har

# Scratch space for local captures and other private debugging material.
/priv/

# Docker
docker-compose.override.yml

# Logs
logs/

# Showcase runner scratch space (raw WebM, traces, scenario state)
front/showcase/.artifacts/

# Frontend build artifacts (copied from front/dist/)
internal/api/resources/index.html
internal/api/resources/assets/index-*

*.backup

# Help charts
dbbat-*.tgz

bin/
tmp/
